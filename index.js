// Code your solutions in this file
const writeCards = (names, eventName) => {
    for(let i = 0; i < names.length; i++) {
        const newArray = [];
        newArray.pop(names[i]);
        return newArray;
    }
}